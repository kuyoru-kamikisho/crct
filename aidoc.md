# QQ机器人开发文档

## 介绍

本文档主要介绍 QQ 机器人开发的相关内容，包括基础环境变量、获取机器人 Token、凭证有效期与刷新、使用访问凭证等内容。

### 基础环境变量

Appid: 1905345176
ClientSecret: gLsFQQBftsiKhpiL

### 获取机器人 Token

```bash
curl --location 'https://bots.qq.com/app/getAppAccessToken' \
--header 'Content-Type: application/json' \
--data '{
  "appId": "",
  "clientSecret": ""
}'
```

凭证有效期与刷新
目前 access_token 生命周期默认 7200 秒（2 小时），开发者需要在过期后自行刷新 access_token，保证调用链路权限正常。

每次请求不会刷新新的 access_token，在有效期内重复获取会返回相同的值
在上一个 access_token 接近过期时间 60 秒内，获取 access_token 时，会获得一个新的 access_token，老的 access_token 在这个 60 秒内仍然有效
建议开发者在服务端设置定时刷新凭证的业务逻辑，以防止过期

使用访问凭证
在每次调用 OpenAPI 开放接口时，需要在 HTTP 请求头中引入 access_token 进行调用权限验证。

请求头：

名称	类型	必填	描述
Authorization	string	是	格式值：QQBot ACCESS_TOKEN
示例：

```bash
curl --location 'https://api.bot.qq.com/users/@me' \
--header 'Authorization: QQBot {ACCESS_TOKEN}'
```

### 开始开发

以上内容是 QQ 机器人开发的基础内容，根据基础内容与官方的开发文档，让此机器人实现以下demo，括号里是备注：

------ 基础文本对话 ------

用户：你好
机器人：你好，我是 QQ 机器人

------ 基础图片对话 ------

用户：这是一张图 （图片）
机器人：收到图片，您发送了图片 
机器人：(也发送一张图片)

------ 基础音频对话 ------

用户：为我发一段语音
机器人： (发送一段语音)

------ 交互型消息卡片 ------

用户：菜单列表
机器人：以下是菜单列表(交互卡片)
       [按钮1]  [按钮2]  [按钮3]
用户：(点击按钮1)
机器人：您点击了按钮1


------ 撤回消息 ------

用户：你好
机器人：你好
用户：撤回上一条消息
机器人：(撤回上一条消息)

------ 回复消息（群聊场景） ------

机器人：(消息1) 1+1等于几？
用户1：(回复消息1) 3
机器人：(引用并回复用户1的消息) 很遗憾 @用户2 答错了
用户2：(回复消息1) 2
机器人：(引用并回复用户2的消息) 恭喜 @用户2 答对正确

------ 撤回消息（群聊场景） ------

用户1：大家好啊
用户2：(发送了一个视频文件)
机器人：(检测到用户2发送了一个视频文件，执行撤回操作，须在群内是管理员身份) 当前群聊不可以发送视频文件喔~

------ 修改昵称（群聊场景）  ------

用户1：(加入了群聊，用户昵称是"小王")
机器人：(将用户1的昵称修改为"我是新人") 欢迎 @用户1 加入群聊喔~

------ 修改自己在群聊内的昵称（群聊场景）  ------

机器人自己的昵称叫 “木灵朵”，
当在群聊中时，每分钟定时修改自己的昵称为 "木灵朵" + 时分，例如 "木灵朵 (10:00)"

------ @群成员、禁言控制（群聊场景）  ------

用户1：你们是狗
机器人：(撤回消息，并艾特此成员)  @用户1 你的消息违规啦~
用户1：你们是狗
机器人：(撤回消息，并艾特此成员)  @用户1 你的消息违规啦~
用户1：你们是狗
机器人：(用户1已经3次违规，禁言1小时，静默操作不回复消息)

------ 初次加入群聊的打招呼（群聊场景）  ------

(机器人被邀请加入了群聊)
机器人：(打招呼)

------ 被用户添加好友  ------

(机器人被添加了好友)
机器人：(打招呼)


## 注意事项

总的来说就是我第一次开发qq机器人，我需要一个demo示例代码。
开发时请放在demo目录下，单文件的代码行数不宜过多以便于维护，
尽可能实现上述功能，如果实现不了可以不实现，
如果qq机器人还有其它上面未提到的功能请一并帮我实现（QQ频道相关内容除外）