server:
  port: 8080

spring:
  datasource:
    url: "jdbc:mysql://:3306/sakiko?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
    username: kuyoru
    password: ""
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
